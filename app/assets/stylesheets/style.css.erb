body {
    background-color: #252525;
    font-family: "Segoe UI", "Roboto Condensed", sans-serif !important;
    color: white;
    margin: 1em;
}

h1,
h2 {
    margin: 0;
}

input, textarea,
.uneditable-input {
    width: 40em;
}

h2 {
    font-size: 1em;
    font-style: italic;
}

a,
a:active,
a:visited {
    display: inline-block;
    text-decoration: none;
    color: inherit;
}

a.btn {
    color: #252525;
}